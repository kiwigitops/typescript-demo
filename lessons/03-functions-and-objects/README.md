# 03 - Functions And Objects

Most TypeScript code is about describing the shape of data moving through functions.

This lesson covers:

- object types
- optional properties
- readonly fields
- function input and output types
- returning a new object instead of mutating an old one

Run it:

```bash
pnpm lesson:03
```

