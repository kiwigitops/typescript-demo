# 01 - Hello World

This lesson shows the smallest useful TypeScript program:

- a typed function parameter
- an inferred return value
- a simple object type
- a script you can run from the terminal

Run it:

```bash
pnpm lesson:01
```

Try changing `learnerName` to a number. The compiler should complain before the program runs.

