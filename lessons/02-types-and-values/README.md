# 02 - Types And Values

TypeScript starts with JavaScript values, then adds a static description of what those values are allowed to be.

This lesson covers:

- primitive types
- literal unions
- arrays
- tuples
- readonly arrays

Run it:

```bash
pnpm lesson:02
```

Notice that `Difficulty` is not any string. It is exactly one of the allowed labels.

