# 05 - Generics And Utilities

Generics are how you write reusable code without throwing away useful type information.

This lesson covers:

- generic functions
- generic response envelopes
- `Pick`, `Partial`, and `Readonly`
- grouping data without losing key types

Run it:

```bash
pnpm lesson:05
```

