# 06 - Async And Errors

Async code fails. A `Result` union makes that visible without throwing exceptions everywhere.

This lesson covers:

- `Promise`
- `async` / `await`
- success and failure unions
- narrowing async results

Run it:

```bash
pnpm lesson:06
```

