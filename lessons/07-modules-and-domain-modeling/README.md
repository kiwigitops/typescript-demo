# 07 - Modules And Domain Modeling

Once your types are useful, move them into modules that the rest of the app can share.

This lesson imports the shared roadmap model from `src/shared/roadmap.ts`, then uses those types and functions like real application code.

Run it:

```bash
pnpm lesson:07
```

The same shared module is later used by the API, frontend, and tests.

