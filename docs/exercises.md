# Exercises

Use these after each lesson once the default command runs cleanly.

## Foundations

- Add a second greeting to chapter 01.
- Add a new track to chapter 02 and update the tuple.

## Type System

- Add a required `priority` field to the task in chapter 03.
- Add an `empty` state to chapter 04 and update the label function.
- Change chapter 05 so `first` returns a fallback when the array is empty.

## Runtime

- Change chapter 06 so the async function sometimes returns `{ ok: false }`.
- Add a second valid role to chapter 07 and reject everything else.

## App

- Add a `theme` field to chapter 08.
- Start `pnpm api`, open `http://127.0.0.1:4174/lessons`, and inspect the JSON.
